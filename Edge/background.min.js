chrome.commands.onCommand.addListener(function(command) { 
    console.log('alan');
    
    //Get the current tab's url, if it's bing, we'll pull out the query and run it on google
    chrome.tabs.getSelected(null,function(tab) {
        console.log('alan2');
        var tablink = tab.url;
        
        var vars = [], hash;
        var hashes = tablink.slice(tablink.indexOf('?') + 1).split('&');
        for(var i = 0; i < hashes.length; i++)
        {
          hash = hashes[i].split('=');
          vars.push(hash[0]);
          vars[hash[0]] = hash[1];
        }

        var query = vars['q'];
        if(query != undefined){
            browser.windows.create("https://www.google.com/?#q="+query.replace("+"," "));
        }
    });
    
});